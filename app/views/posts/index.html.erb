<main>
  <h2 class="font-extrabold text-2xl mb-4">Hello world</h2>
  <% @blog_posts.each do |blog_post|%>
    <div class="mb-6">
      <h2 ><%= blog_post.title %></h2>
      <p><%= blog_post.body %></p>
    </div>
  <% end %>
</main>